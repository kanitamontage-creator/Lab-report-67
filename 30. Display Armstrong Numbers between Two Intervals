#include <stdio.h>
int main() {
    int low, high, n, temp, rem, result;
    printf("Enter two numbers: ");
    scanf("%d %d", &low, &high);

    printf("Armstrong numbers between %d and %d are: ", low, high);

    for (n = low; n <= high; n++) {
        temp = n;
        result = 0;
        while (temp != 0) {
            rem = temp % 10;
            result += rem * rem * rem;
            temp /= 10;
        }
        if (result == n)
            printf("%d ", n);
    }

    return 0;
}